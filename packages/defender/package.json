{
  "name": "taterdao-openzeppelin-defender",
  "license": "GPLv3",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc -p src",
    "test": "jest",
    "autotask:build:index-contract-arbitrum": "yarn ncc build src/autotasks/index-contract-arbitrum/index.ts -o dist/autotasks/index-contract-arbitrum -m",
    "autotask:build:index-contract-arbitrum-testnet": "yarn ncc build src/autotasks/index-contract-arbitrum-testnet/index.ts -o dist/autotasks/index-contract-arbitrum-testnet -m",
    "autotask:build:sync-goerli": "yarn ncc build src/autotasks/sync-goerli/index.ts -o dist/autotasks/sync-goerli -m",
    "autotask:deploy": "yarn build && ts-node scripts/upload-autotask.ts",
    "scripts:backfill:replica": "ts-node scripts/backfill-replica.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^18.6.3",
    "@vercel/ncc": "^0.34.0",
    "defender-autotask-client": "^1.28.1",
    "jest": "^29.0.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^28.0.8",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "algoliasearch": "^4.14.2",
    "defender-autotask-utils": "^1.25.0",
    "defender-relay-client": "^1.26.1",
    "dotenv": "^16.0.1",
    "web3": "^1.7.5"
  }
}
